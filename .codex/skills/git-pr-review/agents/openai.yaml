interface:
  display_name: "Git PR Review"
  short_description: "Inspect a GitHub PR and produce an evidence-backed code review."
  default_prompt: "Use git-pr-review to inspect the current pull request, summarize scope/checks/diff, and produce a prioritized review without posting comments unless explicitly requested."
