# Session 2026-05-27 Handoff — NSSF Phase 1 진입 직전

> **목적.** 본 세션 (2026-05-26 ~ 27 새벽) 의 종결 상태와 다음 PC (회사) 에서의 *즉시 재개* 절차를 self-contained 로 보존. 다음 Claude 가 본 conversation 없이도 이 doc + git log + `docs/handover.md` 의 memory 만으로 재개 가능해야 함.

---

## TL;DR

- **Status** — `ready_for_next_phase` (Phase 1 tracer-bullet 진입 직전, `cmake` install 만 남음).
- **Main HEAD** — `6e3990b` (PR #85 머지 후). 본 세션 wrap-up PR 머지 후 갱신됨.
- **본 세션 PR 머지** — `#70` ~ `#85`. doc/policy/tool 정리 + 16 PR. NSSF `readiness_pack_ready` PASS 유지.
- **다음 PR** — NSSF Phase 1 tracer-bullet (`NSSelectionGet` 501 stub). hand-written stub 이라 openapi-generator 불요. **cmake 만 설치 후 진입 가능**.

---

## 1. 새 PC 첫 행동 체크리스트

```bash
# (1) repo clone + sync
git clone https://github.com/jjinri/5gc-impl-kb.git
cd 5gc-impl-kb
git pull --ff-only      # 본 세션 종결 후 push 까지 최신화

# (2) Python venv 재생성 (PC 별 .venv 는 git 비추적)
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
# requirements.txt = pypdf python-docx pyyaml pytest>=8.0 markdown>=3.10 pygments>=2.20

# (3) auto-memory 재생성 — docs/handover.md §1 의 5개 memory file + MEMORY.md
#     (또는 첫 Claude 세션에서 "memory 재구성" 요청)
DIR=~/.claude/projects/-home-$USER-AI-5gc-impl-kb/memory
mkdir -p "$DIR"
# docs/handover.md 의 §1.1 ~ §1.6 markdown 블록 cp.

# (4) (선택) mattpocock skill 13개 user-global 설치 — docs/setup.md §1.

# (5) Phase 1 진입 필수 — cmake 설치 (1회).
sudo apt-get install -y cmake

# (6) Phase 0 readiness gate 재확인.
.venv/bin/python3 design/scripts/nf-readiness-status.py nssf --no-write
# 기대 — gate readiness_pack_ready: PASS

# (7) pytest 회귀 확인.
.venv/bin/python3 -m pytest tests/ -q
# 기대 — 94 passed (또는 그 이상, validator/test 추가 시).
```

위 7 step 후 Phase 1 PR 진입.

---

## 2. 본 세션 (2026-05-26 ~ 27) 누적 변경

| # | 분류 | 제목 |
|---|---|---|
| #70 | bug fix | `fix(nf-impl-status)` security-baseline loader explicit error reporting |
| #71 | test fix | `test(build-handoff-v2)` drop live NSSF cache dependency — tmp_path fixture 화 |
| #72 | refactor | `refactor(skills)` legacy `/nf-init`·`/nf-build`·`/nf-status` alias 표면 제거 |
| #73 | D1 | `docs(adr-0001)` slim → lifecycle.yaml |
| #74 | OD-H | `chore(od-h)` validator dead reference 정리 |
| #75 | D2 | `docs(adr-0004)` slim → security-baseline.yaml |
| #76 | D3-eng | `docs(adr-0002/0003)` cross-ref to lifecycle.yaml + engineering-core-slots.yaml |
| #77 | CI | `ci(readiness-check)` paths 필터 + pytest scope 확대 + .venv 하드코딩 정리 |
| #78 | docs | history 제거 (README/ONBOARDING/CLAUDE) |
| #79 | docs | `docs/lifecycle-artifacts.md` single-glance catalog (md + html) |
| #80 | docs | README/ONBOARDING 에서 catalog 링크 |
| #81 | tool | `docs(tool)` `render-doc.py` — md → self-contained GitHub-style HTML |
| #82 | docs | README/ONBOARDING/CONTEXT html mirror |
| #83 | D3-sec | `docs(adr-0004)` D3 잔여 — Source precedence + Spec dependency yaml cross-ref |
| #84 | CI | `ci(catalog)` `validate-artifact-catalog.py` — drift guard |
| #85 | refactor | `refactor(scripts)` `nf-status.py` → `nf-contract-check.py` |

### 결과 요약

- **모든 project-level ADR (0001/0002/0003/0004) normative 가 machine-readable yaml 단일 출처** (lifecycle.yaml / security-baseline.yaml / engineering-core-slots.yaml / source-precedence.yaml / spec-dependencies.yaml). ADR 본문은 rationale + history only.
- **CI scope 확대** — `tests/**` + `.claude/skills/**` paths + full pytest (94+ tests). `validate-artifact-catalog.py` drift guard 추가.
- **Onboarding 자산** — `docs/lifecycle-artifacts.md` (+ html) catalog, README/ONBOARDING/CONTEXT html mirror, `render-doc.py` build tool.
- **Surface 정합** — legacy alias 표면 0건, `nf-status.py` → `nf-contract-check.py` rename.

---

## 3. NSSF Phase 1 tracer-bullet — 실행 계획

### 3.1 SKILL `/nf-implement nssf --phase 1` 의 산출 contract

`/nf-implement` SKILL §"Phase 1" 인용.

> 목적 — *가장 작은 vertical slice* 로 전체 toolchain (source layout / generated boundary / build / test) 이 연결됨을 증명.
> Phase 1 산출 contract.
> - source layout / bootstrap 존재 (예 `src/<nf>/`, `CMakeLists.txt`, build manifest).
> - generated / API stub boundary 1 개 (예 `generated/<nf>/api/<Op>.h`).
> - 1 endpoint stub (NSSF 의 경우 `NSSelectionGet`) — 합의된 stub response (ProblemDetails / 501 / agreed value) 반환.
> - 1~2 smoke / unit / contract test.
> - build + test command 실행 및 PASS evidence.
> - *spec semantic rediscovery 부재* evidence — run state 의 `spec_reread_audit` 가 비어있거나 `allowed` 만.

### 3.2 정확한 deliverable

| 파일 | 내용 |
|---|---|
| `CMakeLists.txt` (root) | project(nssf) + add_subdirectory(src/nssf) + add_subdirectory(tests/nssf). c standard, sanitizer flag (engineering-design 의 consequence). |
| `src/nssf/CMakeLists.txt` | nssf binary target. api/ generated/ common/ subdir. |
| `src/nssf/api/nsselection_handler.{h,c}` | NSSelectionGet handler stub. 501 ProblemDetails JSON 반환. cJSON 의존은 *후속 PR* (현재는 hardcoded string). |
| `src/nssf/generated/api/NSSelectionGet.h` | hand-written stub — "이 헤더는 generator 가 만들 것" 의 boundary 표현. WI-codegen-bootstrap 진입 시 실제 generated 헤더로 교체. |
| `tests/nssf/CMakeLists.txt` | Unity test target. add_test. |
| `tests/nssf/vendor/Unity/unity.{h,c}` | Unity 단위 test 프레임워크 vendored (pinned version). engineering-design `test_build_tooling.decision` 의무. |
| `tests/nssf/unit/test_nsselection_handler.c` | smoke test — handler 호출 → 501 응답 string 검증. |
| `.github/workflows/readiness-check.yml` | cmake + ctest step 추가. apt-get install cmake build-essential. |
| `dev/nssf/_implementation_run_state.yaml` | `tracer_bullet_passed: true`, `completed_phases: [1]`, `spec_reread_audit: []`. *비추적* (run state policy). |

### 3.3 명시적 비범위 (Phase 1 외)

- **WI-codegen-bootstrap proper** — openapi-generator-cli (Java) + JRE install + 14 schema 자동 generation. *Phase 3 feature wave* 진입 시.
- **TLS / OAuth2 / mTLS** — `WI-tls-bootstrap`, `WI-problem-details-wrapper`, `WI-nftype-wrapper` 등. Phase 3.
- **persistence (PostgreSQL)** — `WI-availability-repo`, `WI-schema-bootstrap`. Phase 3.
- **실 selection 로직** — `WI-selection-engine`, `WI-nsselection-handler` proper. Phase 3.

본 Phase 1 은 *503 stub* 만으로 충분.

### 3.4 사전 결정 필요 사항

다음 사항은 본 세션이 *남기는 결정 공백*. 새 PC 에서 Phase 1 진입 직전 확정.

1. **Unity 버전 pin** — engineering-design `test_build_tooling` 는 "Unity" 만 명시. 구체 버전 미정. Unity 2.6.0 (안정) 추천.
2. **C standard 선택** — engineering-design `language.decision` 확인. C11 or C17 추천.
3. **CMake minimum version** — 3.22+ (ubuntu 22.04 LTS 기본).
4. **NSSelectionGet 501 응답 정확한 본문** — `Content-Type: application/problem+json` + `{"type": "...", "title": "Not Implemented", "status": 501, "detail": "Phase 1 tracer-bullet stub"}` 권장.
5. **`spec_reread_audit` 의 yaml schema** — `dev/<nf>/_implementation_run_state.yaml` 의 첫 인스턴스라 schema 합의 필요. SKILL §7 (`spec_reread_audit`) 의 contract 따름.

각 결정은 새 세션에서 사용자 한 줄 확인 또는 합리적 기본값으로 진행.

---

## 4. 결정·인계된 정책

### 4.1 신규 메모리 (`project_no_jvm.md`)

JVM/Java 정책. NF runtime + 생성 C 코드 + linked libs 에는 JVM 금지. build-time tool 의 host 언어는 자유. 상세 — `docs/handover.md` §1.5.

### 4.2 ADR 슬림화 패턴

본 세션이 확립한 패턴.
- ADR = *rationale + decision context + migration history*.
- yaml mirror = *machine-readable normative truth*.
- yaml `rationale_source` 가 ADR 을 reverse pointer 로 cross-ref.

향후 새 ADR 작성 시 같은 패턴 적용. yaml mirror 가 *codegen agent 의 read target*.

### 4.3 Single-source HTML pattern

`render-doc.py` 도구로 md → html. md 가 1차, html 은 build artifact. 새 doc 의 html mirror 가 필요하면.

```bash
.venv/bin/python3 design/scripts/render-doc.py <md-path>
```

---

## 5. 남은 follow-ups (다음 세션 후 후보)

### 5.1 Phase 1 직후

- **WI-codegen-bootstrap proper PR** — openapi-generator-cli setup + 14 schema generation. JRE install 포함. Phase 3 feature wave 진입 신호.
- **engineering-design.md `schema_codegen.tool.version`** — 현재 `version_policy: pinned` 만 명시, 구체 version 없음. openapi-generator-cli 채택 시점에 version 확정 + ratify 필요.

### 5.2 Framework 확장 (선택)

- **두 번째 NF (NRF / AMF / UDM 중 1개) `/nf-readiness <nf>` 사이클** — framework NF-invariant 검증. Phase 1 진행 *전* 또는 *후* 의 선택.
- **deterministic-readiness-regeneration plan §Phase E** — clean rebuild proof on backup worktree (`docs/kb/deterministic-readiness-regeneration.md`).

### 5.3 본 세션 metaphysics — 검토 권고

- **`/nf-implement` Phase 0~5 contract 자체 검증** — 본 세션이 Phase 1 진입 직전까지만 진행. Phase 2 (task graph normalization) 의 자동 산출 (예 `dev/<nf>/_implementation_run_state.yaml` 의 task graph 영역) schema 가 미정의일 수 있음. SKILL 본문 보강 가능성.

---

## 6. 본 세션이 보존하지 않는 것

- 이번 conversation 의 in-flight 추론 — 본 doc + git log + ADR/policy yaml + memory 로 충분히 재구성 가능 (장담).
- 다른 PC 의 .venv / pip cache / .pytest_cache — PC-local, gitignored. 재생성 필요.
- mattpocock skill 13개 — `docs/setup.md` §1 의 한 줄 명령 (`/usr/bin/ln -sfn` 루프) 으로 재설치.
- claude.md 환경 설정 (statusline 등) — 사용자 dotfiles 책임.

---

## 7. 자동 검증 (새 PC 에서 step 5 후 즉시 가능)

```bash
.venv/bin/python3 -m pytest tests/ -q
# 기대 — N passed (현재 94+, 본 wrap PR 후 동일).

.venv/bin/python3 design/scripts/nf-readiness-status.py nssf --no-write | tail -3
# 기대 — gate readiness_pack_ready: PASS  blocked_by: []

.venv/bin/python3 design/scripts/validate-artifact-catalog.py
# 기대 — catalog ↔ repo consistent. PASS N FAIL 0.

cmake -S . -B build && cmake --build build  # 본 Phase 1 PR 머지 후만 의미 있음.
```

본 세 명령이 모두 PASS 이면 새 PC 환경 정합 + 본 세션 회귀 없음 확정.

---

**End of handoff.** 새 PC 세션이 본 doc 의 §1 → §3 → §5 순으로 읽으면 즉시 진입 가능.
